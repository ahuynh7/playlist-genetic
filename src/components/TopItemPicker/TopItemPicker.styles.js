import { ListGroup as CustomListGroup } from "react-bootstrap";
import styled from "styled-components";

export const ListGroup = styled(CustomListGroup)`
    justify-content: center;
    width: 69%;
    margin: auto;
`;